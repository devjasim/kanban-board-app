import React from "react";
import "./BoardItem.styles.scss";
const BoardItem = () => {
  const item = "Anyway";

  const newItem = "another";

  return (
    <div>
      <div>BoardItem</div>
      {newItem}
    </div>
  );
};

export default BoardItem;
